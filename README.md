# Kestrel-Vault
Kestrel Vault is a mobile-first secure vault for sensitive data that supports encrypted, client-side backups to cloud storage. Backups are encrypted locally and uploaded to the app's private Google Drive appDataFolder using Google Sign-In, ensuring the cloud provider only ever stores ciphertext and has no access to the passphrase or derived keys.
